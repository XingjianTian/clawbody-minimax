services:
  clawbody:
    environment:
      PYTHONPATH: /app/src
    volumes:
      - type: bind
        source: ./src
        target: /app/src
        read_only: true
      - type: bind
        source: ./robot_identity
        target: /app/robot_identity
        read_only: true
